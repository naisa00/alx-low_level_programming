#include<stdio.h>
/**
  *name - main entry point
  *Return : always 0 (success)
  */
int main(void)
{
         printf("with proper grammar, but the outcome is a piece of airt,/n");
         return (0);
}
